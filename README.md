# HashWarden
HashWarden is a lightweight, pure-Python File Integrity Monitor (FIM). It uses SHA-256 hashing to establish a known-good baseline of a directory and continuously monitors for unauthorized file modifications, additions, or deletions. Features zero dependencies and memory-efficient file reading for large security environments.
